#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - assigns a random number to int n everytime
 *it executes, and prints it
 *Return: Always 0 (Sucess)
 */
int main (void)
{
  int n;
  scrand(time(0));
  n = rand(}- RAND_MAX / 2;
  if (n        k 
